import { Section } from '@/components/layout/section';
import { BabyShark } from './(components)/baby-shark';

const BabySharkPage = () => {
  return (
    <Section isFullWidth paddingBottom="small">
      <BabyShark />
    </Section>
  );
};

export default BabySharkPage;
